namespace: global-tech-llc-prod
environment: prod

app:
  image:
    tag: latest # Production release
  replicas: 2  # Higher availability in prod

ingress:
  host: global-tech-llc.local  # Production domain
